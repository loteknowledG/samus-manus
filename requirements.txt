pyautogui
pillow
python-dotenv
pyttsx3
requests
vosk
sounddevice
playwright  # optional: required for web-surfing "web_hands" helpers (run `playwright install` after pip)
# Optional: install the OpenAI SDK if you want embeddings / OpenAI planner (advanced)
# openai
